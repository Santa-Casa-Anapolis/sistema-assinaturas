{
  "name": "nota-fiscais-assinatura",
  "version": "1.0.0",
  "description": "Sistema de fluxo de assinaturas para notas fiscais com integração GOV.BR",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build:jenkins",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": [
    "assinatura",
    "nota-fiscal",
    "gov-br",
    "fluxo"
  ],
  "author": "Sistema Nota Fiscais",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1"
  }
}
